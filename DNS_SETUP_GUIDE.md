# 🌐 إعداد DNS - دليل شامل

## ✅ الخطوات في Hostinger:

### 1️⃣ أضف A Record للـ Admin Subdomain:

في الحقول التي تراها:

- **Type:** `A` ✅ (صحيح)
- **Name:** `admin` ✅ (صحيح)
- **Points to:** **يجب أن يكون IP السيرفر الصحيح**

⚠️ **التحقق من IP السيرفر:**

في السيرفر، نفذ:
```bash
curl ifconfig.me
```

أو استخدم:
- `84.32.84.32` (من رسالة الخطأ)
- أو `2a02:4780:28:4342::1` (IPv6 من رسالة النشر)

**استخدم IPv4 (مثل 84.32.84.32)**

---

### 2️⃣ بعد إضافة السجل:

1. اضغط على زر **"Add Record"** البنفسجي
2. انتظر 10-15 دقيقة لانتشار DNS

---

### 3️⃣ التحقق من DNS:

```bash
nslookup admin.luzasculture.org
```

يجب أن يظهر IP السيرفر.

---

### 4️⃣ بعد انتشار DNS:

احصل على SSL:
```bash
certbot --nginx -d admin.luzasculture.org --email orders@luzasculture.org --agree-tos --non-interactive
```

---

## 📋 جميع السجلات المطلوبة:

1. ✅ `luzasculture.org` → IP السيرفر (A Record)
2. ✅ `www.luzasculture.org` → IP السيرفر (A Record أو CNAME)
3. ⏳ `admin.luzasculture.org` → IP السيرفر (A Record) **هذا ناقص!**

---

**استخدم IP السيرفر الصحيح في حقل "Points to"!**


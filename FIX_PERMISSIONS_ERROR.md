# 🔧 حل مشكلة الصلاحيات

## ❌ المشكلة:
المشروع موجود في المسار الخطأ: `/root/var/www/luzasculture`
يجب أن يكون في: `/var/www/luzasculture`

## ✅ الحل - خطوات الإصلاح:

### الخطوة 1: الخروج من المجلد الحالي
```bash
cd /
```

### الخطوة 2: حذف المجلد الخاطئ (اختياري - أو اتركه)
```bash
# إذا أردت حذفه:
rm -rf /root/var/www/luzasculture
```

### الخطوة 3: الانتقال للمسار الصحيح
```bash
cd /var/www
```

### الخطوة 4: التأكد أنك في المسار الصحيح
```bash
pwd
```
يجب أن يظهر: `/var/www`

### الخطوة 5: تنزيل المشروع في المسار الصحيح
```bash
git clone https://github.com/MahmouT1/luzaend2.git luzasculture
```

### الخطوة 6: الدخول للمجلد
```bash
cd luzasculture
```

### الخطوة 7: التأكد من المسار
```bash
pwd
```
يجب أن يظهر: `/var/www/luzasculture`

### الخطوة 8: تشغيل السكربت
```bash
chmod +x deploy.sh
sudo ./deploy.sh
```

---

## 📋 الأوامر السريعة (انسخها):

```bash
cd /var/www
git clone https://github.com/MahmouT1/luzaend2.git luzasculture
cd luzasculture
chmod +x deploy.sh
sudo ./deploy.sh
```

---

**المشكلة:** المشروع في `/root/var/www` بدلاً من `/var/www` ✅


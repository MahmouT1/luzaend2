# 📧 تفعيل نظام الإيميلات على السيرفر

## ⚠️ المشكلة
نظام الإيميلات يعمل على localhost ولكن لا يعمل على السيرفر.

## ✅ الحل

### 1️⃣ التحقق من ملف `.env` على السيرفر

تأكد من وجود الملف التالي:
```
/var/www/luzasculture/loza-server-master/loza-server-master/.env
```

### 2️⃣ إضافة المتغيرات المطلوبة

يجب أن يحتوي ملف `.env` على المتغيرات التالية:

```env
EMAIL_USER=orders@luzasculture.org
EMAIL_PASS=YOUR_EMAIL_PASSWORD_HERE
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
```

**ملاحظة:** استبدل `YOUR_EMAIL_PASSWORD_HERE` بكلمة مرور الإيميل الفعلية.

### 3️⃣ الخطوات على السيرفر

#### أ) فتح ملف `.env`:
```bash
cd /var/www/luzasculture/loza-server-master/loza-server-master
nano .env
```

#### ب) إضافة/تحديث المتغيرات:
```env
EMAIL_USER=orders@luzasculture.org
EMAIL_PASS=Memo.Ro2123
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
```

#### ج) حفظ الملف:
- اضغط `Ctrl + X`
- اضغط `Y` للتحفظ
- اضغط `Enter` للتأكيد

### 4️⃣ إعادة تشغيل الـ Server

```bash
cd /var/www/luzasculture
pm2 restart luzasculture-server
pm2 save
```

### 5️⃣ التحقق من عمل النظام

#### أ) تحقق من logs:
```bash
pm2 logs luzasculture-server
```

#### ب) ابحث عن رسائل الإيميل في الـ logs:
```
📧 Creating Hostinger SMTP transporter:
SMTP Host: smtp.hostinger.com
SMTP Port: 465
Email User: orders@luzasculture.org
Email Pass configured: Yes
```

### 6️⃣ اختبار الإيميلات

عند إنشاء طلب جديد، يجب أن:
1. يتم إرسال إيميل تأكيد للعميل
2. يتم إرسال إشعار لصاحب المتجر (`Alysherif771@gmail.com`)

### 7️⃣ حل المشاكل الشائعة

#### المشكلة 1: لا توجد رسائل في الـ logs
**الحل:** تحقق من أن `.env` موجود وأن PM2 يقوم بتحميل المتغيرات.

#### المشكلة 2: خطأ في الاتصال بـ SMTP
**الحل:** جرب تغيير `SMTP_PORT` إلى `587`:
```env
SMTP_PORT=587
```

#### المشكلة 3: خطأ في المصادقة
**الحل:** تحقق من كلمة مرور الإيميل في Hostinger.

---

## 📋 قائمة التحقق

- [ ] ملف `.env` موجود في `/var/www/luzasculture/loza-server-master/loza-server-master/`
- [ ] المتغيرات `EMAIL_USER`, `EMAIL_PASS`, `SMTP_HOST`, `SMTP_PORT` موجودة
- [ ] تم إعادة تشغيل `luzasculture-server` عبر PM2
- [ ] تم التحقق من الـ logs
- [ ] تم اختبار إنشاء طلب جديد

---

## 🔒 الأمان

- ⚠️ **لا تشارك** ملف `.env` أو كلمة مرور الإيميل
- ⚠️ تأكد من أن `.env` موجود في `.gitignore`
- ⚠️ لا ترفع ملف `.env` إلى GitHub

---

**✅ بعد إضافة المتغيرات وإعادة التشغيل، يجب أن يعمل نظام الإيميلات!**


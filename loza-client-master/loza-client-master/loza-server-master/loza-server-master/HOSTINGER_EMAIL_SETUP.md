# Hostinger Email Setup Guide

## متطلبات الإعداد

تم تحديث نظام الإيميلات لاستخدام Hostinger Business Email SMTP بدلاً من Gmail.

## المتغيرات البيئية المطلوبة

أضف المتغيرات التالية إلى ملف `.env` في مجلد `loza-server-master`:

```env
# Hostinger SMTP Configuration
EMAIL_USER=orders@luzasculture.org
EMAIL_PASS=your-hostinger-email-password
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
```

### ملاحظات مهمة:

1. **EMAIL_USER**: الإيميل الرسمي للموقع (`orders@luzasculture.org`)
2. **EMAIL_PASS**: كلمة مرور إيميل Hostinger Business Email
3. **SMTP_HOST**: `smtp.hostinger.com` (افتراضي)
4. **SMTP_PORT**: `465` (افتراضي - Secure SSL)

## كيفية الحصول على كلمة المرور:

1. سجل الدخول إلى لوحة تحكم Hostinger
2. اذهب إلى **Email** → **Email Accounts**
3. اختر الحساب `orders@luzasculture.org`
4. اضغط على **Configure Mail Client** أو **Access Webmail**
5. استخدم كلمة المرور الموجودة أو قم بإنشاء كلمة مرور جديدة للتطبيقات

## اختبار الإيميلات:

بعد إضافة المتغيرات البيئية:

1. أعد تشغيل الـ Server
2. قم بعمل عملية شراء من الموقع
3. تحقق من وصول إيميل التأكيد إلى صندوق الوارد

## الأمان:

- **لا تشارك** ملف `.env` أو المتغيرات البيئية
- تأكد من أن ملف `.env` موجود في `.gitignore`
- استخدم كلمة مرور قوية لحساب الإيميل

## اختبار الإيميلات:

بعد إضافة المتغيرات البيئية، يمكنك اختبار الإيميلات باستخدام:

```bash
node test-hostinger-email.js
```

هذا الأمر سيقوم بـ:
1. التحقق من المتغيرات البيئية
2. اختبار الاتصال بـ SMTP
3. إرسال إيميل تأكيد باستخدام آخر طلب في قاعدة البيانات

## المشاكل الشائعة:

### الإيميل لا يصل:
1. **تحقق من المتغيرات البيئية:**
   - افتح ملف `.env` في مجلد `loza-server-master`
   - تأكد من وجود جميع المتغيرات:
     ```
     EMAIL_USER=orders@luzasculture.org
     EMAIL_PASS=your-password-here
     SMTP_HOST=smtp.hostinger.com
     SMTP_PORT=465
     ```

2. **أعد تشغيل الـ Server:**
   - بعد إضافة/تعديل المتغيرات البيئية، يجب إعادة تشغيل الـ Server

3. **تحقق من Console logs:**
   - عند إنشاء طلب، تحقق من Console logs في Terminal
   - ابحث عن رسائل تبدأ بـ 📧 أو ❌
   - ستجد تفاصيل المشكلة هناك

4. **اختبار الاتصال:**
   ```bash
   node test-hostinger-email.js
   ```

5. **تحقق من كلمة المرور:**
   - تأكد من أن كلمة المرور صحيحة
   - في Hostinger، قد تحتاج إلى كلمة مرور خاصة للتطبيقات
   - تأكد من أن الحساب نشط

6. **تحقق من Firewall:**
   - تأكد من أن المنفذ 465 مفتوح
   - تأكد من أن Server يمكنه الوصول إلى `smtp.hostinger.com`

### الإيميل يذهب إلى Spam:
- تأكد من أن إعدادات SPF و DKIM صحيحة في Hostinger
- استخدم الإيميل الرسمي `orders@luzasculture.org`
- تحقق من أن اسم المرسل "LUZA'S CULTURE" واضح

### رسالة خطأ: "EMAIL_PASS environment variable is required":
- هذا يعني أن المتغير البيئي `EMAIL_PASS` غير موجود
- أضف المتغير إلى ملف `.env` وأعد تشغيل الـ Server


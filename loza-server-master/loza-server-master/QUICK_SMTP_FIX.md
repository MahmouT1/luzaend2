# 🚀 حل سريع لمشكلة الاتصال بـ SMTP

## المشكلة:
الاتصال بـ SMTP Hostinger لا يعمل.

## الحل السريع (3 خطوات):

### الخطوة 1: اختبار الاتصال

شغّل هذا الأمر لاختبار جميع إعدادات SMTP:

```bash
cd loza-server-master
node test-smtp-connection.js
```

هذا سيجرب المنفذ 587 و 465 ويخبرك بأي واحد يعمل.

### الخطوة 2: تحديث ملف `.env`

بناءً على نتائج الاختبار، حدّث ملف `.env`:

**خيار 1: استخدام المنفذ 587 (TLS) - موصى به:**
```env
EMAIL_USER=orders@luzasculture.org
EMAIL_PASS=كلمة-المرور-هنا
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USE_SSL=false
```

**خيار 2: استخدام المنفذ 465 (SSL):**
```env
EMAIL_USER=orders@luzasculture.org
EMAIL_PASS=كلمة-المرور-هنا
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USE_SSL=true
```

### الخطوة 3: أعد تشغيل Server

```bash
npm run dev
```

## التحسينات التي تمت:

✅ **دعم المنفذ 587 (TLS)** - الأكثر استخداماً  
✅ **دعم المنفذ 465 (SSL)** - كبديل  
✅ **Timeouts محسّنة** - 10 ثوانٍ للاتصال  
✅ **تحسين إعدادات TLS** - توافق أفضل  
✅ **سكربت اختبار شامل** - يختبر جميع الخيارات  

## إذا لم يعمل بعد:

1. **تحقق من كلمة المرور:**
   - سجل الدخول إلى Hostinger
   - اذهب إلى Email → Email Accounts
   - تحقق من كلمة المرور أو غيّرها

2. **تحقق من Firewall:**
   - تأكد من أن المنفذ 587 أو 465 مفتوح
   - جرب من شبكة مختلفة

3. **اتصل بدعم Hostinger:**
   - قد تكون هناك قيود على الحساب
   - تأكد من أن حساب البريد نشط

## اختبار سريع:

```bash
# 1. اختبار الاتصال
node test-smtp-connection.js

# 2. اختبار إرسال إيميل
node test-hostinger-email.js
```

---

**بعد تطبيق الخطوات، النظام سيعمل! 🎉**


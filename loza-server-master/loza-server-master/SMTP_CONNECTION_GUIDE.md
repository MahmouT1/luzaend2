# 🔧 دليل إصلاح مشكلة الاتصال بـ SMTP Hostinger

## المشكلة:
الاتصال بـ SMTP Hostinger لا يعمل، والمشكلة في الاتصال وليس في الكود.

## الحلول الموصى بها:

### 1. استخدام المنفذ 587 (TLS) بدلاً من 465 (SSL)

في كثير من الأحيان، المنفذ 587 يعمل بشكل أفضل. قم بتحديث ملف `.env`:

```env
EMAIL_USER=orders@luzasculture.org
EMAIL_PASS=كلمة-المرور-هنا
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USE_SSL=false
```

### 2. إذا لم يعمل 587، جرب المنفذ 465 (SSL):

```env
EMAIL_USER=orders@luzasculture.org
EMAIL_PASS=كلمة-المرور-هنا
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USE_SSL=true
```

### 3. اختبار الاتصال:

قم بتشغيل:

```bash
node test-smtp-connection.js
```

هذا سيقوم باختبار جميع المنافذ (587 و 465) ويخبرك بأي واحد يعمل.

## خطوات حل المشكلة:

### الخطوة 1: التحقق من بيانات الاعتماد

1. سجل الدخول إلى Hostinger
2. اذهب إلى **Email** → **Email Accounts**
3. تأكد من أن `orders@luzasculture.org` نشط
4. قم بتغيير كلمة المرور إذا لزم الأمر

### الخطوة 2: اختبار الاتصال

```bash
cd loza-server-master
node test-smtp-connection.js
```

هذا سيعطيك معلومات مفصلة عن أي منفذ يعمل.

### الخطوة 3: تحديث ملف `.env`

بناءً على نتائج الاختبار، قم بتحديث `.env`:

```env
EMAIL_USER=orders@luzasculture.org
EMAIL_PASS=كلمة-المرور-الصحيحة
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587  # أو 465 حسب النتائج
```

### الخطوة 4: أعد تشغيل Server

```bash
npm run dev
```

### الخطوة 5: اختبر الإيميل

```bash
node test-hostinger-email.js
```

## المشاكل الشائعة وحلولها:

### ❌ المشكلة: "Connection timeout"
**الحل:**
- تحقق من أن Firewall يسمح بالاتصال إلى المنفذ 587 أو 465
- جرب تغيير المنفذ

### ❌ المشكلة: "Invalid login"
**الحل:**
- تحقق من كلمة المرور مرة أخرى
- تأكد من نسخها بشكل صحيح (بدون مسافات)
- جرب تغيير كلمة المرور في Hostinger

### ❌ المشكلة: "ECONNREFUSED"
**الحل:**
- تحقق من أن `smtp.hostinger.com` صحيح
- تأكد من الاتصال بالإنترنت
- تحقق من Firewall

### ❌ المشكلة: "Certificate error"
**الحل:**
- الكود الآن يتجاهل أخطاء الشهادة (`rejectUnauthorized: false`)
- إذا استمرت المشكلة، تحقق من إعدادات TLS

## إعدادات SMTP المحدثة:

الكود الآن يدعم:
- ✅ المنفذ 587 (TLS) - **موصى به**
- ✅ المنفذ 465 (SSL) - بديل
- ✅ Timeouts محسّنة (10 ثوانٍ)
- ✅ TLS options محسّنة
- ✅ Debug logging مفعّل

## اختبار سريع:

1. شغّل: `node test-smtp-connection.js`
2. إذا نجح الاختبار، سترى رسالة نجاح
3. إذا فشل، ستحصل على تفاصيل الخطأ

## ملاحظات مهمة:

- ⚠️ **المنفذ 587 (TLS)** هو الأفضل عادةً
- ⚠️ تأكد من أن Firewall يسمح بالاتصال
- ⚠️ تحقق من كلمة المرور في Hostinger
- ⚠️ جرب تغيير كلمة المرور إذا لزم الأمر

---

**بعد تطبيق الحلول، شغّل `node test-smtp-connection.js` لاختبار الاتصال!**


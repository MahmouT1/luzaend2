# أوامر تشخيص وإصلاح مشكلة التشغيل

# 1. التحقق من وجود ملف .next (البناء)
ls -la .next | head -5

# 2. التحقق من package.json و scripts
cat package.json | grep -A 5 '"scripts"'

# 3. محاولة تشغيل يدوي لمعرفة الخطأ
cd /root/var/www/luzasculture/loza-client-master/loza-client-master
npm run start 2>&1 | head -20

# 4. إذا كان هناك خطأ، التحقق من node_modules
# npm install

# 5. إعادة التشغيل بشكل صحيح
# lsof -ti :3000 | xargs kill -9 2>/dev/null
# sleep 2
# cd /root/var/www/luzasculture/loza-client-master/loza-client-master
# nohup npm run start > /tmp/loza-client.log 2>&1 &
# sleep 5
# tail -20 /tmp/loza-client.log



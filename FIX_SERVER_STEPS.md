# 🔧 إصلاح المشكلة على السيرفر

## ❌ المشكلة:
المشروع لم يتم تنزيله بعد على السيرفر.

## ✅ الحل - الأوامر الصحيحة:

### الخطوة 1: التحقق من المجلد الحالي
```bash
pwd
```
يجب أن تكون في: `/var/www`

### الخطوة 2: تنزيل المشروع (هذا ما ناقص!)
```bash
git clone https://github.com/MahmouT1/luzaend2.git luzasculture
```

انتظر حتى يكتمل التنزيل (سيأخذ دقيقة أو دقيقتين).

### الخطوة 3: الدخول للمجلد
```bash
cd luzasculture
```

### الخطوة 4: التحقق من وجود السكربت
```bash
ls -la deploy.sh
```
يجب أن ترى الملف موجود.

### الخطوة 5: جعل السكربت قابل للتشغيل
```bash
chmod +x deploy.sh
```

### الخطوة 6: تشغيل السكربت
```bash
sudo ./deploy.sh
```

---

## 📋 ملخص الأوامر (انسخها بالترتيب):

```bash
cd /var/www
git clone https://github.com/MahmouT1/luzaend2.git luzasculture
cd luzasculture
chmod +x deploy.sh
sudo ./deploy.sh
```

---

**المشكلة:** نسيان تنفيذ `git clone` أولاً! ✅


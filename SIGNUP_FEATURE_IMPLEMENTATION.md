# ✅ تنفيذ خاصية Signup في صفحة Login

## 🎯 المطلوب:
- عند الضغط على "Sign up" في صفحة Login، تتغير الفورم من Login إلى Register
- نفس التصميم والمقاس بالظبط
- حقول: الاسم، الإيميل، الباسورد وزر Register
- إنشاء حساب فعلي

---

## ✅ ما تم تنفيذه:

### 1. تعديل صفحة Login (`src/app/login/page.tsx`)
- ✅ إضافة state `isRegisterMode` للتبديل بين Login و Register
- ✅ إنشاء form منفصل للـ Register مع validation
- ✅ حقول Register: Name, Email, Password
- ✅ زر "Sign up" يغير الفورم إلى Register
- ✅ زر "Sign in" يعيد الفورم إلى Login
- ✅ نفس التصميم والمقاس بالظبط

### 2. ربط Register API
- ✅ استخدام `useRegisterMutation` من Redux
- ✅ إرسال البيانات: `{ name, email, password }`
- ✅ بعد التسجيل الناجح، يتم تسجيل الدخول تلقائياً
- ✅ إظهار رسالة نجاح و redirect

---

## 📋 التغييرات:

### الملفات المعدلة:
1. `src/app/login/page.tsx` - إضافة خاصية التبديل بين Login و Register
2. `src/redux/features/auth/authApi.js` - تحسين رسائل النجاح (تم بالفعل)

---

## 🧪 الاختبار على Localhost:

### الخطوة 1: تشغيل المشروع

```bash
cd loza-client-master/loza-client-master
npm run dev
```

### الخطوة 2: اختبار Login
1. افتح: `http://localhost:3000/login`
2. يجب أن تظهر صفحة Login بشكل طبيعي

### الخطوة 3: اختبار التبديل إلى Register
1. اضغط على "Sign up" في أسفل الصفحة
2. يجب أن تتغير الفورم إلى Register مع حقول:
   - Name
   - Email
   - Password
3. نفس التصميم والمقاس

### الخطوة 4: اختبار إنشاء حساب
1. املأ الحقول:
   - Name: Test User
   - Email: test@example.com
   - Password: password123
2. اضغط على "Register"
3. يجب أن يتم إنشاء الحساب وتسجيل الدخول تلقائياً

---

## 🚀 رفع التعديلات على GitHub:

### بعد التأكد من أن كل شيء يعمل على localhost:

```bash
cd "C:\loza website"
git add .
git commit -m "Add signup form toggle in login page"
git push origin main
```

---

## 📝 ملاحظات:
- Google Sign In يظهر فقط في صفحة Login (ليس في Register)
- بعد التسجيل الناجح، يتم تسجيل الدخول تلقائياً
- رسائل الخطأ تظهر بشكل واضح

---

## ✅ الخطوات التالية:
1. ✅ اختبار على localhost
2. ⏳ رفع التعديلات على GitHub
3. ⏳ تطبيق التعديلات على السيرفر

---

**🎉 جاهز للاختبار!**


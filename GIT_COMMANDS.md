# 🚀 أوامر رفع التعديلات على Git

## 📋 الأوامر الكاملة (انسخها كلها):

```bash
cd "C:\loza website"
git add .
git commit -m "Fix email system on server and add bestsellers endpoint"
git push origin main
```

---

## 📝 شرح الأوامر خطوة بخطوة:

### 1️⃣ الانتقال لمجلد المشروع:
```bash
cd "C:\loza website"
```

### 2️⃣ إضافة جميع التعديلات:
```bash
git add .
```

### 3️⃣ حفظ التعديلات مع رسالة:
```bash
git commit -m "Fix email system on server and add bestsellers endpoint"
```

### 4️⃣ رفع التعديلات على GitHub:
```bash
git push origin main
```

---

## ✅ بعد رفع التعديلات:

### على السيرفر:

```bash
ssh root@luzasculture.org
cd /var/www/luzasculture
git pull origin main
```

---

**🎉 جاهز!**

